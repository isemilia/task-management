import { styled, Box } from "@mui/material";

export const CardFrame = styled(Box)(
    () => ({
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between'
    })
);